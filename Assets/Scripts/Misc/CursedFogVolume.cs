using UnityEngine;

public class CursedFogVolume : MonoBehaviour
{
    // This script acts as a marker so the player knows they entered a cursed fog.
    // Attach this to the CursedFogEffect prefab alongside the BoxCollider (IsTrigger = true).
}
